{
  "name": "cc-cockpit",
  "private": true,
  "version": "0.1.0",
  "description": "CC Cockpit — macOS desktop cockpit for Claude Code (Tauri + SolidJS over tmux). Run: npm install && npm run dev",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
